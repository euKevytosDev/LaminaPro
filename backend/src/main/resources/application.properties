spring.application.name=LaminaPro
server.port=${PORT:8080}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

app.jwt.secret=${APP_JWT_SECRET:laminapro-segredo-dev-mude-em-producao-1234567890}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:7776000000}
app.google.client-id=${GOOGLE_CLIENT_ID:868389533637-d3l4a0mrnnbf7i1h34cd0mts996sb6pc.apps.googleusercontent.com}

# Dono padrão (criado no seed se não existir)
app.seed.dono.email=${APP_SEED_DONO_EMAIL:dono@laminapro.app}
app.seed.dono.senha=${APP_SEED_DONO_SENHA:dono123}
app.seed.dono.nome=${APP_SEED_DONO_NOME:Dono LaminaPro}

# Lâmina Pro SaaS / Mercado Pago
app.public-url=${APP_PUBLIC_URL:https://eukevytosdev.github.io/LaminaPro}
app.assinatura.webhook-url=${APP_ASSINATURA_WEBHOOK_URL:}
app.assinatura.trial-dias=${APP_ASSINATURA_TRIAL_DIAS:14}
app.mercadopago.access-token=${MERCADOPAGO_ACCESS_TOKEN:}
app.mercadopago.public-key=${MERCADOPAGO_PUBLIC_KEY:}
